import 'package:flutter/material.dart';

class SetMenuPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Set Menu'),
      ),
      body: Center(
        child: Text('Set Menu Page'),
      ),
    );
  }
}
